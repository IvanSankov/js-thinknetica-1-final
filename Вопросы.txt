1. В общем, возникли проблемы с переменной templateIssueBlock из main.ts, так как document.getElementById
    не возвращал HTMLTemplateElement, а возращал HTMLElement, то, как заставить его возращать нужное мне значение
    на просторах интернета я не нашел, но, покапался в каком-то файле node_modules/typescript/lib/lib.dom.d.ts. Вроде
    понял, что это дженерик, наверное и поэтому сделал:
        const templateIssueBlock: HTMLTemplateElement = <HTMLTemplateElement>document.getElementById('js-issue');
    тогда все завелось. Но на 100% не понимаю почему это так. А хочу понять :-)
2. А еще меня немного смущает моя типизация возращаемого значения в Github.listRepositoryIssues
3. А когда ts преобразуется в js, к js потом babel применяется?